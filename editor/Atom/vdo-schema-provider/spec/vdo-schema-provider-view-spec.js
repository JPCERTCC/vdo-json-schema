'use babel';

import VdoSchemaProviderView from '../lib/vdo-schema-provider-view';

describe('VdoSchemaProviderView', () => {
  it('has one valid test', () => {
    expect('life').toBe('easy');
  });
});
